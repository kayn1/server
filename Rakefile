# froen_strin_lieral: true

require_relative 'server'
require_relative 'client'

# This is a Rakefile, similar to makefile
desc 'It will run simple TCP server'
task :server do
  Server.start
end

desc 'It will run simple TCP client'
task :client do
  Client.start
end
